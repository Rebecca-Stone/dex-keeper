services:
  - type: web
    name: dex-keeper-api
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
      - key: CORS_ORIGIN
        fromService:
          type: web
          name: dex-keeper
          property: host

  - type: web
    name: dex-keeper
    runtime: static
    buildCommand: npm install && (test -n "$VITE_API_URL" || (echo "VITE_API_URL is empty — is the dex-keeper-api service deployed?" && false)) && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: dex-keeper-api
          property: host
